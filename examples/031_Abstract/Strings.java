public abstract class Strings extends Instrument {
	private int numberOfStrings;
	
	// Parameterized method
    public Strings(String name, int numStrings)
	{
		super(name);
		numberOfStrings = numStrings;
	}

}
